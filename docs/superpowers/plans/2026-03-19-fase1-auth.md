# Fase 1 — Auth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar autenticação completa em ambas as APIs — management-api (Firebase Admin + JWT superadmin + RoleGuard) e app-api (Firebase Admin + JWT gestor/motorista + TenantGuard + refresh token) — com `POST /auth/login` funcional em produção.

**Architecture:** Cada API possui seu próprio `FirebaseModule` (singleton), `AuthModule` com `JwtStrategy` (Passport), guards por responsabilidade (`RolesGuard` na management-api; `TenantGuard` na app-api), e decorators de contexto (`@CurrentUser()`, `@TenantId()`). O `packages/shared/` provê os tipos `JwtPayload` e `UserRole` compartilhados. A management-api acessa `app.users` via `$queryRaw` (cross-schema sem FK) para lookup de superadmin. O refresh token na app-api é JWT stateless com expiry longa (sem tabela nova).

**Tech Stack:** `firebase-admin`, `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `@types/passport-jwt`, `class-validator`, `class-transformer`.

**Pré-requisito:** Fase 0 concluída — Prisma Client gerado para ambas as APIs (tabela `app.users` existe no banco). Ver Task 0 (verificação obrigatória antes de qualquer código).

**Spec:** `docs/superpowers/specs/2026-03-19-backend-roadmap-design.md` § Fase 1
**Vault:** `RouttesApp_Core/CORE/Arquitetura técnica.md` § Fluxo de autenticação

---

## Task 0: Verificação de pré-requisitos (obrigatória antes de qualquer código)

- [ ] **0.1** Confirmar que `PrismaService` e `PrismaModule` existem em ambas as APIs:

```bash
ls services/management-api/src/prisma/
ls services/app-api/src/prisma/
```

Expected: ambas as pastas contêm `prisma.service.ts` e `prisma.module.ts`. Se não existirem, voltar à Fase 0 antes de continuar.

- [ ] **0.2** Confirmar que o Prisma Client da app-api tem o model `users` (tabela criada pela migration 001):

```bash
grep -n "model users" services/app-api/src/prisma/schema.prisma
```

Expected: linha com `model users {` encontrada. Se não, executar `prisma db pull` novamente.

- [ ] **0.3** Confirmar que o `DATABASE_URL` da management-api tem permissão de `SELECT` no schema `app` (cross-schema query para superadmin lookup). O Neon cria um role padrão `neondb_owner` com acesso a todos os schemas. Verificar:

```bash
# Via Neon MCP — confirmar role do connection string
# Executar query de teste no branch production:
SELECT usename, has_schema_privilege(usename, 'app', 'USAGE') AS can_use_app
FROM pg_user
WHERE usename = current_user;
```

Se `can_use_app = false`, executar no Neon: `GRANT USAGE ON SCHEMA app TO <role>; GRANT SELECT ON app.users TO <role>;`

- [ ] **0.4** Confirmar que `useGlobalPipes(new ValidationPipe(...))` está em `main.ts` de ambas as APIs:

```bash
grep -n "ValidationPipe" services/management-api/src/main.ts
grep -n "ValidationPipe" services/app-api/src/main.ts
```

Expected: linha com `new ValidationPipe` em cada arquivo. Se ausente, adicionar antes de continuar (bug da Fase 0).

- [ ] **0.5** Confirmar que `tsconfig.json` de ambas as APIs tem `paths` para `@routtes/shared`. Se não tiver, adicionar agora:

Em `services/management-api/tsconfig.json`, dentro de `compilerOptions`:
```json
"paths": {
  "@routtes/shared": ["../../packages/shared/src/index.ts"]
}
```

Em `services/app-api/tsconfig.json`, mesma adição.

Também verificar que `packages/shared/package.json` tem `"name": "@routtes/shared"` — se não tiver, adicionar.

- [ ] **0.6** Verificar compilação atual (linha base):

```bash
cd services/management-api && pnpm exec tsc --noEmit
cd services/app-api && pnpm exec tsc --noEmit
```

Expected: zero erros (Fase 0 produz código limpo). Se houver erros pré-existentes, anotar e resolver antes de continuar.

---

## Arquivos a criar / modificar

```
packages/shared/src/
├── types/
│   ├── jwt-payload.type.ts          ← interface JwtPayload (sub, tenantId, role)
│   └── user-role.enum.ts            ← enum UserRole (superadmin, manager, driver, guardian)
└── index.ts                         ← re-exports (MODIFY)

services/management-api/src/
├── auth/
│   ├── auth.module.ts               ← imports FirebaseModule, JwtModule, PassportModule
│   ├── auth.controller.ts           ← POST /auth/login
│   ├── auth.service.ts              ← verifyFirebaseToken + $queryRaw superadmin + signJwt
│   ├── auth.service.spec.ts         ← unit tests com mocks
│   ├── firebase/
│   │   ├── firebase.module.ts       ← provê FIREBASE_APP singleton
│   │   └── firebase.service.ts     ← verifyIdToken (captura erros → 401)
│   ├── strategies/
│   │   └── jwt.strategy.ts         ← PassportStrategy(Strategy), valida JWT
│   ├── guards/
│   │   ├── jwt-auth.guard.ts       ← extends AuthGuard('jwt')
│   │   └── roles.guard.ts          ← verifica payload.role via Reflector
│   ├── decorators/
│   │   ├── roles.decorator.ts      ← @Roles(UserRole.SUPERADMIN)
│   │   └── current-user.decorator.ts ← @CurrentUser() param decorator
│   └── dto/
│       └── login.dto.ts            ← { idToken: string }
└── app.module.ts                   ← MODIFY: adicionar AuthModule

services/app-api/src/
├── auth/
│   ├── auth.module.ts
│   ├── auth.controller.ts           ← POST /auth/login, POST /auth/refresh
│   ├── auth.service.ts              ← login + refresh (JWT stateless)
│   ├── auth.service.spec.ts
│   ├── firebase/
│   │   ├── firebase.module.ts
│   │   └── firebase.service.ts
│   ├── strategies/
│   │   └── jwt.strategy.ts         ← injeta userId, tenantId, role em request.user
│   ├── guards/
│   │   ├── jwt-auth.guard.ts
│   │   └── tenant.guard.ts         ← garante tenantId presente, bloqueia superadmin
│   ├── decorators/
│   │   ├── current-user.decorator.ts
│   │   └── tenant-id.decorator.ts  ← @TenantId() extrai tenantId do request
│   └── dto/
│       ├── login.dto.ts
│       └── refresh.dto.ts          ← { refreshToken: string }
└── app.module.ts                   ← MODIFY: adicionar AuthModule
```

---

## Task 1: Tipos compartilhados em packages/shared

**Files:**
- Create: `packages/shared/src/types/user-role.enum.ts`
- Create: `packages/shared/src/types/jwt-payload.type.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **1.1** Criar `packages/shared/src/types/user-role.enum.ts`:

```typescript
export enum UserRole {
  SUPERADMIN = 'superadmin',
  MANAGER = 'manager',
  DRIVER = 'driver',
  GUARDIAN = 'guardian',
}
```

- [ ] **1.2** Criar `packages/shared/src/types/jwt-payload.type.ts`:

```typescript
import { UserRole } from './user-role.enum';

export interface JwtPayload {
  sub: string;       // user.id (uuid)
  tenantId: string | null;  // null para superadmin
  role: UserRole;
  iat?: number;
  exp?: number;
}
```

- [ ] **1.3** Atualizar `packages/shared/src/index.ts` para re-exportar:

```typescript
export { UserRole } from './types/user-role.enum';
export type { JwtPayload } from './types/jwt-payload.type';
```

- [ ] **1.4** Verificar que `packages/shared/package.json` tem `"main": "src/index.ts"` e `"exports"` corretos. Se não tiver, adicionar:

```json
{
  "name": "@routtes/shared",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
```

- [ ] **1.5** Commit:

```bash
git add packages/shared/src/types/ packages/shared/src/index.ts packages/shared/package.json
git commit -m "feat(shared): add UserRole enum and JwtPayload interface"
```

---

## Task 2: Instalar dependências de auth em ambas as APIs

**Files:**
- Modify: `services/management-api/package.json`
- Modify: `services/app-api/package.json`

- [ ] **2.1** Adicionar dependências em `services/management-api/package.json`:

```json
{
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/passport": "^10.0.3",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "class-validator": "^0.14.1",
    "class-transformer": "^0.5.1"
  },
  "devDependencies": {
    "@types/passport-jwt": "^4.0.1"
  }
}
```

> **Nota:** `class-validator` e `class-transformer` podem já estar presentes da Fase 0 (ValidationPipe global os requer). Verificar antes de duplicar. Se já estiverem no `package.json`, não adicionar novamente.

- [ ] **2.2** Repetir o mesmo em `services/app-api/package.json` (mesmas dependências).

- [ ] **2.3** Instalar na raiz do monorepo:

```bash
pnpm install
```

Expected: sem erros, `node_modules` atualizado em ambos os serviços.

- [ ] **2.4** Commit:

```bash
git add services/management-api/package.json services/app-api/package.json pnpm-lock.yaml
git commit -m "feat(auth): add firebase-admin, passport-jwt dependencies to both APIs"
```

---

## Task 3: management-api — FirebaseModule

**Files:**
- Create: `services/management-api/src/auth/firebase/firebase.module.ts`
- Create: `services/management-api/src/auth/firebase/firebase.service.ts`

- [ ] **3.1** Criar `services/management-api/src/auth/firebase/firebase.service.ts`:

```typescript
import { Inject, Injectable, UnauthorizedException } from '@nestjs/common';
import * as admin from 'firebase-admin';

@Injectable()
export class FirebaseService {
  constructor(@Inject('FIREBASE_APP') private readonly app: admin.app.App) {}

  async verifyIdToken(idToken: string): Promise<admin.auth.DecodedIdToken> {
    try {
      return await this.app.auth().verifyIdToken(idToken);
    } catch {
      throw new UnauthorizedException('Invalid Firebase ID token');
    }
  }
}
```

- [ ] **3.2** Criar `services/management-api/src/auth/firebase/firebase.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import * as admin from 'firebase-admin';
import { FirebaseService } from './firebase.service';

@Module({
  imports: [ConfigModule],
  providers: [
    {
      provide: 'FIREBASE_APP',
      useFactory: (config: ConfigService): admin.app.App => {
        if (admin.apps.length > 0) return admin.app();
        return admin.initializeApp({
          credential: admin.credential.cert({
            projectId: config.get<string>('FIREBASE_PROJECT_ID'),
            clientEmail: config.get<string>('FIREBASE_CLIENT_EMAIL'),
            privateKey: config
              .get<string>('FIREBASE_PRIVATE_KEY')
              .replace(/\\n/g, '\n'),
          }),
        });
      },
      inject: [ConfigService],
    },
    FirebaseService,
  ],
  exports: [FirebaseService],
})
export class FirebaseModule {}
```

- [ ] **3.3** Verificar que TypeScript compila sem erros:

```bash
cd services/management-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **3.4** Commit:

```bash
git add services/management-api/src/auth/firebase/
git commit -m "feat(management-api): add FirebaseModule with singleton app initialization"
```

---

## Task 4: management-api — AuthService com testes

**Files:**
- Create: `services/management-api/src/auth/dto/login.dto.ts`
- Create: `services/management-api/src/auth/auth.service.ts`
- Create: `services/management-api/src/auth/auth.service.spec.ts`

- [ ] **4.1** Criar `services/management-api/src/auth/dto/login.dto.ts`:

```typescript
import { IsString, IsNotEmpty } from 'class-validator';

export class LoginDto {
  @IsString()
  @IsNotEmpty()
  idToken: string;
}
```

- [ ] **4.2** Escrever `services/management-api/src/auth/auth.service.spec.ts` (failing test primeiro):

```typescript
import { Test } from '@nestjs/testing';
import { UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { AuthService } from './auth.service';
import { FirebaseService } from './firebase/firebase.service';
import { PrismaService } from '../prisma/prisma.service';
import { UserRole } from '@routtes/shared';

describe('AuthService (management-api)', () => {
  let service: AuthService;
  let firebase: jest.Mocked<FirebaseService>;
  let prisma: jest.Mocked<PrismaService>;
  let jwtService: jest.Mocked<JwtService>;

  const mockSuperadmin = {
    id: 'user-uuid-1',
    name: 'Admin Routtes',
    email: 'admin@routtes.app',
    role: 'superadmin',
    status: 'active',
  };

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [
        AuthService,
        {
          provide: FirebaseService,
          useValue: { verifyIdToken: jest.fn() },
        },
        {
          provide: PrismaService,
          useValue: { $queryRaw: jest.fn() },
        },
        {
          provide: JwtService,
          useValue: { sign: jest.fn() },
        },
        {
          provide: ConfigService,
          useValue: { get: jest.fn() },
        },
      ],
    }).compile();

    service = module.get(AuthService);
    firebase = module.get(FirebaseService);
    prisma = module.get(PrismaService);
    jwtService = module.get(JwtService);
  });

  it('should return accessToken for valid superadmin', async () => {
    firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-1' } as any);
    (prisma.$queryRaw as jest.Mock).mockResolvedValue([mockSuperadmin]);
    jwtService.sign.mockReturnValue('signed-jwt');

    const result = await service.login('valid-idtoken');

    expect(result.accessToken).toBe('signed-jwt');
    expect(result.user.role).toBe(UserRole.SUPERADMIN);
    expect(firebase.verifyIdToken).toHaveBeenCalledWith('valid-idtoken');
  });

  it('should throw UnauthorizedException when user not found in DB', async () => {
    firebase.verifyIdToken.mockResolvedValue({ uid: 'unknown-uid' } as any);
    (prisma.$queryRaw as jest.Mock).mockResolvedValue([]);

    await expect(service.login('valid-idtoken')).rejects.toThrow(UnauthorizedException);
  });

  it('should throw UnauthorizedException when Firebase token is invalid', async () => {
    firebase.verifyIdToken.mockRejectedValue(new UnauthorizedException());

    await expect(service.login('bad-token')).rejects.toThrow(UnauthorizedException);
  });

  it('should throw UnauthorizedException when superadmin is inactive', async () => {
    firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-1' } as any);
    (prisma.$queryRaw as jest.Mock).mockResolvedValue([{ ...mockSuperadmin, status: 'inactive' }]);

    await expect(service.login('valid-idtoken')).rejects.toThrow(UnauthorizedException);
  });

  // RF08.3: gestor/motorista não consegue login na management-api
  it('should throw UnauthorizedException when a manager tries login (query returns empty — role filter)', async () => {
    firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-manager' } as any);
    // The $queryRaw filters WHERE role = 'superadmin', so a manager's firebase_uid returns []
    (prisma.$queryRaw as jest.Mock).mockResolvedValue([]);

    await expect(service.login('manager-idtoken')).rejects.toThrow(UnauthorizedException);
  });
});
```

- [ ] **4.3** Rodar testes para confirmar que falham (AuthService não existe ainda):

```bash
cd services/management-api && pnpm test -- --testPathPattern=auth.service
```

Expected: FAIL — "Cannot find module './auth.service'"

- [ ] **4.4** Criar `services/management-api/src/auth/auth.service.ts`:

```typescript
import { Injectable, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { JwtPayload, UserRole } from '@routtes/shared';
import { FirebaseService } from './firebase/firebase.service';
import { PrismaService } from '../prisma/prisma.service';

interface SuperadminRow {
  id: string;
  name: string;
  email: string;
  role: string;
  status: string;
}

@Injectable()
export class AuthService {
  constructor(
    private readonly firebase: FirebaseService,
    private readonly prisma: PrismaService,
    private readonly jwt: JwtService,
    private readonly config: ConfigService,
  ) {}

  async login(idToken: string) {
    const decoded = await this.firebase.verifyIdToken(idToken);

    const rows = await this.prisma.$queryRaw<SuperadminRow[]>`
      SELECT id, name, email, role, status
      FROM app.users
      WHERE firebase_uid = ${decoded.uid}
        AND role = 'superadmin'
      LIMIT 1
    `;

    const user = rows[0];
    if (!user) {
      throw new UnauthorizedException('Superadmin account not found');
    }
    if (user.status !== 'active') {
      throw new UnauthorizedException('Account is inactive');
    }

    const payload: JwtPayload = {
      sub: user.id,
      tenantId: null,
      role: UserRole.SUPERADMIN,
    };

    return {
      accessToken: this.jwt.sign(payload),
      user: {
        id: user.id,
        name: user.name,
        email: user.email,
        role: UserRole.SUPERADMIN,
      },
    };
  }
}
```

- [ ] **4.5** Rodar testes para confirmar que passam:

```bash
cd services/management-api && pnpm test -- --testPathPattern=auth.service
```

Expected: PASS — 5 tests passando.

- [ ] **4.6** Commit:

```bash
git add services/management-api/src/auth/dto/ services/management-api/src/auth/auth.service.ts services/management-api/src/auth/auth.service.spec.ts
git commit -m "feat(management-api): add AuthService with superadmin login and unit tests"
```

---

## Task 5: management-api — JWT Strategy, Guards e Decorators

**Files:**
- Create: `services/management-api/src/auth/strategies/jwt.strategy.ts`
- Create: `services/management-api/src/auth/guards/jwt-auth.guard.ts`
- Create: `services/management-api/src/auth/guards/roles.guard.ts`
- Create: `services/management-api/src/auth/decorators/roles.decorator.ts`
- Create: `services/management-api/src/auth/decorators/current-user.decorator.ts`

- [ ] **5.1** Criar `services/management-api/src/auth/strategies/jwt.strategy.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';
import { JwtPayload } from '@routtes/shared';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: config.get<string>('JWT_SECRET'),
    });
  }

  async validate(payload: JwtPayload): Promise<JwtPayload> {
    return payload;
  }
}
```

- [ ] **5.2** Criar `services/management-api/src/auth/guards/jwt-auth.guard.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

- [ ] **5.3** Criar `services/management-api/src/auth/decorators/roles.decorator.ts`:

```typescript
import { SetMetadata } from '@nestjs/common';
import { UserRole } from '@routtes/shared';

export const ROLES_KEY = 'roles';
export const Roles = (...roles: UserRole[]) => SetMetadata(ROLES_KEY, roles);
```

- [ ] **5.4** Criar `services/management-api/src/auth/guards/roles.guard.ts`:

```typescript
import { Injectable, CanActivate, ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { UserRole, JwtPayload } from '@routtes/shared';
import { ROLES_KEY } from '../decorators/roles.decorator';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.getAllAndOverride<UserRole[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!requiredRoles) return true;
    const user = context.switchToHttp().getRequest().user as JwtPayload;
    return requiredRoles.includes(user?.role);
  }
}
```

- [ ] **5.5** Criar `services/management-api/src/auth/decorators/current-user.decorator.ts`:

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';
import { JwtPayload } from '@routtes/shared';

export const CurrentUser = createParamDecorator(
  (_data: unknown, ctx: ExecutionContext): JwtPayload => {
    return ctx.switchToHttp().getRequest().user;
  },
);
```

- [ ] **5.6** Verificar compilação:

```bash
cd services/management-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **5.7** Commit:

```bash
git add services/management-api/src/auth/strategies/ services/management-api/src/auth/guards/ services/management-api/src/auth/decorators/
git commit -m "feat(management-api): add JwtStrategy, JwtAuthGuard, RolesGuard and decorators"
```

---

## Task 6: management-api — AuthController, AuthModule e wire no AppModule

**Files:**
- Create: `services/management-api/src/auth/auth.controller.ts`
- Create: `services/management-api/src/auth/auth.module.ts`
- Modify: `services/management-api/src/app.module.ts`

- [ ] **6.1** Criar `services/management-api/src/auth/auth.controller.ts`:

```typescript
import { Body, Controller, Post } from '@nestjs/common';
import { AuthService } from './auth.service';
import { LoginDto } from './dto/login.dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('login')
  login(@Body() dto: LoginDto) {
    return this.authService.login(dto.idToken);
  }
}
```

- [ ] **6.2** Criar `services/management-api/src/auth/auth.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { PassportModule } from '@nestjs/passport';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';
import { FirebaseModule } from './firebase/firebase.module';
import { JwtStrategy } from './strategies/jwt.strategy';
import { PrismaModule } from '../prisma/prisma.module';

@Module({
  imports: [
    FirebaseModule,
    PrismaModule,
    PassportModule,
    JwtModule.registerAsync({
      imports: [ConfigModule],
      useFactory: (config: ConfigService) => ({
        secret: config.get<string>('JWT_SECRET'),
        signOptions: { expiresIn: config.get<string>('JWT_EXPIRY', '15m') },
      }),
      inject: [ConfigService],
    }),
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy],
  exports: [JwtStrategy, PassportModule],
})
export class AuthModule {}
```

- [ ] **6.3** Modificar `services/management-api/src/app.module.ts` para importar `AuthModule`:

Localizar o array `imports: [...]` em `app.module.ts` e adicionar `AuthModule`. Exemplo da linha a adicionar (após `PrismaModule` ou `HealthModule`):

```typescript
import { AuthModule } from './auth/auth.module';

// no array imports:
AuthModule,
```

- [ ] **6.4** Verificar compilação completa:

```bash
cd services/management-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **6.5** Rodar todos os testes:

```bash
cd services/management-api && pnpm test
```

Expected: todos passando.

- [ ] **6.6** Commit:

```bash
git add services/management-api/src/auth/auth.controller.ts services/management-api/src/auth/auth.module.ts services/management-api/src/app.module.ts
git commit -m "feat(management-api): wire AuthModule — POST /auth/login live"
```

---

## Task 7: app-api — FirebaseModule

**Files:**
- Create: `services/app-api/src/auth/firebase/firebase.module.ts`
- Create: `services/app-api/src/auth/firebase/firebase.service.ts`

- [ ] **7.1** Criar `services/app-api/src/auth/firebase/firebase.service.ts`:

```typescript
import { Inject, Injectable, UnauthorizedException } from '@nestjs/common';
import * as admin from 'firebase-admin';

@Injectable()
export class FirebaseService {
  constructor(@Inject('FIREBASE_APP') private readonly app: admin.app.App) {}

  async verifyIdToken(idToken: string): Promise<admin.auth.DecodedIdToken> {
    try {
      return await this.app.auth().verifyIdToken(idToken);
    } catch {
      throw new UnauthorizedException('Invalid Firebase ID token');
    }
  }
}
```

- [ ] **7.2** Criar `services/app-api/src/auth/firebase/firebase.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import * as admin from 'firebase-admin';
import { FirebaseService } from './firebase.service';

@Module({
  imports: [ConfigModule],
  providers: [
    {
      provide: 'FIREBASE_APP',
      useFactory: (config: ConfigService): admin.app.App => {
        if (admin.apps.length > 0) return admin.app();
        return admin.initializeApp({
          credential: admin.credential.cert({
            projectId: config.get<string>('FIREBASE_PROJECT_ID'),
            clientEmail: config.get<string>('FIREBASE_CLIENT_EMAIL'),
            privateKey: config
              .get<string>('FIREBASE_PRIVATE_KEY')
              .replace(/\\n/g, '\n'),
          }),
        });
      },
      inject: [ConfigService],
    },
    FirebaseService,
  ],
  exports: [FirebaseService],
})
export class FirebaseModule {}
```

- [ ] **7.3** Verificar compilação:

```bash
cd services/app-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **7.4** Commit:

```bash
git add services/app-api/src/auth/firebase/
git commit -m "feat(app-api): add FirebaseModule with singleton app initialization"
```

---

## Task 8: app-api — AuthService com testes (inclui refresh token)

**Files:**
- Create: `services/app-api/src/auth/dto/login.dto.ts`
- Create: `services/app-api/src/auth/dto/refresh.dto.ts`
- Create: `services/app-api/src/auth/auth.service.ts`
- Create: `services/app-api/src/auth/auth.service.spec.ts`

- [ ] **8.1** Criar `services/app-api/src/auth/dto/login.dto.ts`:

```typescript
import { IsString, IsNotEmpty } from 'class-validator';

export class LoginDto {
  @IsString()
  @IsNotEmpty()
  idToken: string;
}
```

- [ ] **8.2** Criar `services/app-api/src/auth/dto/refresh.dto.ts`:

```typescript
import { IsString, IsNotEmpty } from 'class-validator';

export class RefreshDto {
  @IsString()
  @IsNotEmpty()
  refreshToken: string;
}
```

- [ ] **8.3** Escrever `services/app-api/src/auth/auth.service.spec.ts` (failing tests primeiro):

```typescript
import { Test } from '@nestjs/testing';
import { UnauthorizedException, ForbiddenException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { AuthService } from './auth.service';
import { FirebaseService } from './firebase/firebase.service';
import { PrismaService } from '../prisma/prisma.service';
import { UserRole } from '@routtes/shared';

describe('AuthService (app-api)', () => {
  let service: AuthService;
  let firebase: jest.Mocked<FirebaseService>;
  let prisma: { users: { findUnique: jest.Mock } };
  let jwtService: jest.Mocked<JwtService>;

  const mockManager = {
    id: 'user-uuid-2',
    tenant_id: 'tenant-uuid-1',
    firebase_uid: 'firebase-uid-2',
    role: 'manager',
    name: 'João Gestor',
    email: 'joao@empresa.com',
    status: 'active',
  };

  beforeEach(async () => {
    prisma = { users: { findUnique: jest.fn() } };

    const module = await Test.createTestingModule({
      providers: [
        AuthService,
        { provide: FirebaseService, useValue: { verifyIdToken: jest.fn() } },
        { provide: PrismaService, useValue: prisma },
        { provide: JwtService, useValue: { sign: jest.fn(), verify: jest.fn() } },
        { provide: ConfigService, useValue: { get: jest.fn() } },
      ],
    }).compile();

    service = module.get(AuthService);
    firebase = module.get(FirebaseService);
    jwtService = module.get(JwtService);
  });

  describe('login', () => {
    it('should return accessToken and refreshToken for active manager', async () => {
      firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-2' } as any);
      prisma.users.findUnique.mockResolvedValue(mockManager);
      jwtService.sign.mockReturnValueOnce('access-token').mockReturnValueOnce('refresh-token');

      const result = await service.login('valid-idtoken');

      expect(result.accessToken).toBe('access-token');
      expect(result.refreshToken).toBe('refresh-token');
      expect(result.user.tenantId).toBe('tenant-uuid-1');
    });

    it('should throw UnauthorizedException when user not found', async () => {
      firebase.verifyIdToken.mockResolvedValue({ uid: 'unknown' } as any);
      prisma.users.findUnique.mockResolvedValue(null);

      await expect(service.login('token')).rejects.toThrow(UnauthorizedException);
    });

    it('should throw ForbiddenException when superadmin tries to login', async () => {
      firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-sa' } as any);
      prisma.users.findUnique.mockResolvedValue({ ...mockManager, role: 'superadmin', tenant_id: null });

      await expect(service.login('token')).rejects.toThrow(ForbiddenException);
    });

    it('should throw UnauthorizedException when account is inactive', async () => {
      firebase.verifyIdToken.mockResolvedValue({ uid: 'firebase-uid-2' } as any);
      prisma.users.findUnique.mockResolvedValue({ ...mockManager, status: 'inactive' });

      await expect(service.login('token')).rejects.toThrow(UnauthorizedException);
    });
  });

  describe('refresh', () => {
    it('should return new accessToken for valid refresh token', async () => {
      jwtService.verify.mockReturnValue({ sub: 'user-uuid-2', type: 'refresh' });
      prisma.users.findUnique.mockResolvedValue(mockManager);
      jwtService.sign.mockReturnValue('new-access-token');

      const result = await service.refresh('valid-refresh-token');

      expect(result.accessToken).toBe('new-access-token');
    });

    it('should throw UnauthorizedException for invalid refresh token', async () => {
      jwtService.verify.mockImplementation(() => { throw new Error('expired'); });

      await expect(service.refresh('bad-token')).rejects.toThrow(UnauthorizedException);
    });

    it('should throw UnauthorizedException when token type is not refresh', async () => {
      // Prevent access tokens from being used as refresh tokens
      jwtService.verify.mockReturnValue({ sub: 'user-uuid-2', type: 'access' });

      await expect(service.refresh('access-token-used-as-refresh')).rejects.toThrow(UnauthorizedException);
    });
  });
});
```

- [ ] **8.4** Rodar testes para confirmar que falham:

```bash
cd services/app-api && pnpm test -- --testPathPattern=auth.service
```

Expected: FAIL — "Cannot find module './auth.service'"

- [ ] **8.5** Criar `services/app-api/src/auth/auth.service.ts`:

```typescript
import {
  Injectable,
  UnauthorizedException,
  ForbiddenException,
} from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { JwtPayload, UserRole } from '@routtes/shared';
import { FirebaseService } from './firebase/firebase.service';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class AuthService {
  constructor(
    private readonly firebase: FirebaseService,
    private readonly prisma: PrismaService,
    private readonly jwt: JwtService,
    private readonly config: ConfigService,
  ) {}

  async login(idToken: string) {
    const decoded = await this.firebase.verifyIdToken(idToken);

    const user = await this.prisma.users.findUnique({
      where: { firebase_uid: decoded.uid },
      select: {
        id: true,
        tenant_id: true,
        role: true,
        name: true,
        email: true,
        status: true,
      },
    });

    if (!user) throw new UnauthorizedException('User not found');
    if (user.status !== 'active') throw new UnauthorizedException('Account is inactive');
    if (user.role === 'superadmin') {
      throw new ForbiddenException('SuperAdmin cannot access this API');
    }
    if (!user.tenant_id) throw new UnauthorizedException('User has no tenant context');

    const payload: JwtPayload = {
      sub: user.id,
      tenantId: user.tenant_id,
      role: user.role as UserRole,
    };

    const accessToken = this.jwt.sign(payload, {
      expiresIn: this.config.get<string>('JWT_EXPIRY', '15m'),
    });

    const refreshToken = this.jwt.sign(
      { sub: user.id, type: 'refresh' },
      { expiresIn: this.config.get<string>('JWT_REFRESH_EXPIRY', '7d') },
    );

    return {
      accessToken,
      refreshToken,
      user: {
        id: user.id,
        name: user.name,
        email: user.email,
        role: user.role,
        tenantId: user.tenant_id,
      },
    };
  }

  async refresh(refreshToken: string) {
    let payload: { sub: string; type?: string };
    try {
      payload = this.jwt.verify<{ sub: string; type?: string }>(refreshToken);
    } catch {
      throw new UnauthorizedException('Invalid or expired refresh token');
    }

    if (payload.type !== 'refresh') {
      throw new UnauthorizedException('Token type is not a refresh token');
    }

    const user = await this.prisma.users.findUnique({
      where: { id: payload.sub },
      select: { id: true, tenant_id: true, role: true, status: true },
    });

    if (!user || user.status !== 'active') {
      throw new UnauthorizedException('User not found or inactive');
    }

    const newPayload: JwtPayload = {
      sub: user.id,
      tenantId: user.tenant_id,
      role: user.role as UserRole,
    };

    return {
      accessToken: this.jwt.sign(newPayload, {
        expiresIn: this.config.get<string>('JWT_EXPIRY', '15m'),
      }),
    };
  }
}
```

- [ ] **8.6** Rodar testes para confirmar que passam:

```bash
cd services/app-api && pnpm test -- --testPathPattern=auth.service
```

Expected: PASS — 6 tests passando.

- [ ] **8.7** Commit:

```bash
git add services/app-api/src/auth/dto/ services/app-api/src/auth/auth.service.ts services/app-api/src/auth/auth.service.spec.ts
git commit -m "feat(app-api): add AuthService with manager/driver login, refresh token and unit tests"
```

---

## Task 9: app-api — JWT Strategy, Guards e Decorators

**Files:**
- Create: `services/app-api/src/auth/strategies/jwt.strategy.ts`
- Create: `services/app-api/src/auth/guards/jwt-auth.guard.ts`
- Create: `services/app-api/src/auth/guards/tenant.guard.ts`
- Create: `services/app-api/src/auth/decorators/current-user.decorator.ts`
- Create: `services/app-api/src/auth/decorators/tenant-id.decorator.ts`

- [ ] **9.1** Criar `services/app-api/src/auth/strategies/jwt.strategy.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PassportStrategy } from '@nestjs/passport';
import { ExtractJwt, Strategy } from 'passport-jwt';
import { JwtPayload } from '@routtes/shared';

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: config.get<string>('JWT_SECRET'),
    });
  }

  async validate(payload: JwtPayload): Promise<JwtPayload> {
    return payload;
  }
}
```

- [ ] **9.2** Criar `services/app-api/src/auth/guards/jwt-auth.guard.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

- [ ] **9.3** Criar `services/app-api/src/auth/guards/tenant.guard.ts`:

```typescript
import {
  Injectable,
  CanActivate,
  ExecutionContext,
  UnauthorizedException,
  ForbiddenException,
} from '@nestjs/common';
import { JwtPayload, UserRole } from '@routtes/shared';

@Injectable()
export class TenantGuard implements CanActivate {
  canActivate(context: ExecutionContext): boolean {
    const user = context.switchToHttp().getRequest().user as JwtPayload;

    if (user?.role === UserRole.SUPERADMIN) {
      throw new ForbiddenException('SuperAdmin cannot access operational resources');
    }

    if (!user?.tenantId) {
      throw new UnauthorizedException('Tenant context is required');
    }

    return true;
  }
}
```

- [ ] **9.4** Criar `services/app-api/src/auth/decorators/current-user.decorator.ts`:

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';
import { JwtPayload } from '@routtes/shared';

export const CurrentUser = createParamDecorator(
  (_data: unknown, ctx: ExecutionContext): JwtPayload => {
    return ctx.switchToHttp().getRequest().user;
  },
);
```

- [ ] **9.5** Criar `services/app-api/src/auth/decorators/tenant-id.decorator.ts`:

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common';
import { JwtPayload } from '@routtes/shared';

export const TenantId = createParamDecorator(
  (_data: unknown, ctx: ExecutionContext): string => {
    const user = ctx.switchToHttp().getRequest().user as JwtPayload;
    return user.tenantId as string;
  },
);
```

- [ ] **9.6** Verificar compilação:

```bash
cd services/app-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **9.7** Commit:

```bash
git add services/app-api/src/auth/strategies/ services/app-api/src/auth/guards/ services/app-api/src/auth/decorators/
git commit -m "feat(app-api): add JwtStrategy, JwtAuthGuard, TenantGuard and decorators"
```

---

## Task 10: app-api — AuthController, AuthModule e wire no AppModule

**Files:**
- Create: `services/app-api/src/auth/auth.controller.ts`
- Create: `services/app-api/src/auth/auth.module.ts`
- Modify: `services/app-api/src/app.module.ts`

- [ ] **10.1** Criar `services/app-api/src/auth/auth.controller.ts`:

```typescript
import { Body, Controller, Post } from '@nestjs/common';
import { AuthService } from './auth.service';
import { LoginDto } from './dto/login.dto';
import { RefreshDto } from './dto/refresh.dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('login')
  login(@Body() dto: LoginDto) {
    return this.authService.login(dto.idToken);
  }

  @Post('refresh')
  refresh(@Body() dto: RefreshDto) {
    return this.authService.refresh(dto.refreshToken);
  }
}
```

- [ ] **10.2** Criar `services/app-api/src/auth/auth.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { PassportModule } from '@nestjs/passport';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';
import { FirebaseModule } from './firebase/firebase.module';
import { JwtStrategy } from './strategies/jwt.strategy';
import { PrismaModule } from '../prisma/prisma.module';

@Module({
  imports: [
    FirebaseModule,
    PrismaModule,
    PassportModule,
    JwtModule.registerAsync({
      imports: [ConfigModule],
      useFactory: (config: ConfigService) => ({
        secret: config.get<string>('JWT_SECRET'),
        signOptions: { expiresIn: config.get<string>('JWT_EXPIRY', '15m') },
      }),
      inject: [ConfigService],
    }),
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy],
  exports: [JwtStrategy, PassportModule, JwtModule],
})
export class AuthModule {}
```

- [ ] **10.3** Modificar `services/app-api/src/app.module.ts` para importar `AuthModule`:

```typescript
import { AuthModule } from './auth/auth.module';

// no array imports:
AuthModule,
```

- [ ] **10.4** Verificar compilação completa:

```bash
cd services/app-api && pnpm exec tsc --noEmit
```

Expected: zero erros.

- [ ] **10.5** Rodar todos os testes:

```bash
cd services/app-api && pnpm test
```

Expected: todos passando.

- [ ] **10.6** Commit:

```bash
git add services/app-api/src/auth/auth.controller.ts services/app-api/src/auth/auth.module.ts services/app-api/src/app.module.ts
git commit -m "feat(app-api): wire AuthModule — POST /auth/login and POST /auth/refresh live"
```

---

## Task 11: PR + Deploy + Validação em produção

**Ferramenta:** use-github (PR) + railway:use-railway (deploy e validação)
**Skill:** Ler `RouttesApp_Core/SKILLS/use-github/SKILL.md` antes do PR.
**Skill:** Invocar `railway:use-railway` para cada etapa de deploy.

- [ ] **11.1** Criar PR com branch `feat/fase1-auth` → `main`:

```bash
# Garantir que todos os commits da Fase 1 estão na branch
git log --oneline origin/main..HEAD
```

Expected: listar os commits das Tasks 1-10.

- [ ] **11.2** Abrir PR via GitHub MCP (use-github):
  - Título: `feat: Fase 1 — Auth (management-api + app-api)`
  - Body: descrever o que foi implementado (Firebase Admin, JWT, guards, refresh token)
  - Revisar que não há secrets em código

- [ ] **11.3** Fazer merge do PR em `main` após aprovação.

- [ ] **11.4** Deploy management-api via railway:use-railway:
  - Verificar build logs no Railway
  - Confirmar status 200 em `GET /health` após deploy

- [ ] **11.5** Deploy app-api via railway:use-railway:
  - Verificar build logs no Railway
  - Confirmar status 200 em `GET /health` após deploy

- [ ] **11.5b** Seed: criar usuário superadmin no Firebase e no banco (necessário para os curls de validação abaixo):

  1. Criar usuário no Firebase Console (`routtes-app` project → Authentication → Add user) com e-mail `admin@routtes.app`
  2. Anotar o UID gerado pelo Firebase
  3. Inserir na tabela `app.users` via Neon MCP (substituir `<firebase-uid>` pelo UID real):

```sql
INSERT INTO app.users (id, tenant_id, firebase_uid, role, name, email, status)
VALUES (
  gen_random_uuid(),
  NULL,
  '<firebase-uid>',
  'superadmin',
  'Admin Routtes',
  'admin@routtes.app',
  'active'
);
```

  4. Obter o `idToken` do superadmin: Firebase Console → Authentication → selecionar usuário → Sign in with custom token, ou usar Firebase REST API com `email` e `password`:

```bash
curl -X POST \
  "https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key=<FIREBASE_WEB_API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@routtes.app","password":"<senha-criada>","returnSecureToken":true}'
```

  Salvar o campo `idToken` da resposta para o próximo passo.

- [ ] **11.6** Validar `POST /auth/login` na management-api em produção:

```bash
# Obter um idToken válido de superadmin via Firebase Console → Authentication → Get token
curl -X POST https://<management-api-url>/auth/login \
  -H "Content-Type: application/json" \
  -d '{"idToken": "<firebase-id-token-de-superadmin>"}'
```

Expected:
```json
{
  "accessToken": "eyJ...",
  "user": {
    "id": "...",
    "name": "...",
    "email": "...",
    "role": "superadmin"
  }
}
```

- [ ] **11.6b** Seed: criar um tenant + gestor para validar a app-api (se não existir):

```sql
-- Criar tenant de teste
INSERT INTO management.tenants (id, city, state, status)
VALUES (gen_random_uuid(), 'São Paulo', 'SP', 'active')
RETURNING id;
-- Anotar o tenant_id retornado

-- Criar usuário gestor no Firebase Console (email: gestor@empresa.com)
-- Anotar o UID do Firebase

-- Inserir na app.users (substituir <tenant-id> e <firebase-uid-gestor>)
INSERT INTO app.users (id, tenant_id, firebase_uid, role, name, email, status)
VALUES (
  gen_random_uuid(),
  '<tenant-id>',
  '<firebase-uid-gestor>',
  'manager',
  'João Gestor',
  'gestor@empresa.com',
  'active'
);
```

- [ ] **11.7** Validar `POST /auth/login` na app-api em produção:

```bash
curl -X POST https://<app-api-url>/auth/login \
  -H "Content-Type: application/json" \
  -d '{"idToken": "<firebase-id-token-de-gestor>"}'
```

Expected:
```json
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ...",
  "user": {
    "id": "...",
    "name": "...",
    "email": "...",
    "role": "manager",
    "tenantId": "..."
  }
}
```

- [ ] **11.8** Validar `POST /auth/refresh` na app-api:

```bash
curl -X POST https://<app-api-url>/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken": "<refresh-token-recebido-acima>"}'
```

Expected:
```json
{
  "accessToken": "eyJ..."
}
```

---

## Definição de Pronto

- [ ] `POST /auth/login` retorna JWT válido para superadmin na management-api em produção
- [ ] `POST /auth/login` retorna JWT + refreshToken para gestor/motorista na app-api em produção
- [ ] `POST /auth/refresh` emite novo accessToken na app-api
- [ ] `POST /auth/refresh` com access token (sem `type: 'refresh'`) retorna 401
- [ ] Superadmin recebe 403 ao tentar login na app-api (RF08.4)
- [ ] Gestor/motorista recebe 401 ao tentar login na management-api (RF08.3)
- [ ] Usuário inativo recebe 401
- [ ] Token Firebase inválido recebe 401
- [ ] Todos os testes unitários passam em ambas as APIs
- [ ] `GET /health` continua 200 em produção após deploy
- [ ] Task 0 completada: PrismaModule confirmado, permissões Neon verificadas, `@routtes/shared` paths configurados

---

## Notas de design

**Cross-schema query na management-api:** A management-api usa `prisma.$queryRaw` para consultar `app.users` durante o login do superadmin. Isso ocorre porque management-api só tem acesso ao schema `management` via Prisma models, mas auth precisa verificar o role do usuário. O `$queryRaw` é aceitável aqui pois é read-only e ocorre apenas no login. **Requisito:** o Neon role usado pela management-api deve ter `USAGE` no schema `app` e `SELECT` em `app.users` — verificar na Task 0.3.

**Refresh token stateless com claim `type: 'refresh'`:** O refresh token é um JWT assinado com o mesmo `JWT_SECRET` e expiração em `JWT_REFRESH_EXPIRY` (default 7d), com o claim `{ sub, type: 'refresh' }`. O campo `type` impede que um refresh token seja aceito como access token (o `JwtStrategy` não verifica `type`, mas a `AuthService.refresh()` valida que `type === 'refresh'`). Não é armazenado em banco — o MVP não requer revogação de tokens. Revogação (blacklist) é evolução pós-MVP se necessário.

**`@routtes/shared` import:** A resolução do path `@routtes/shared` depende de `tsconfig.json` ter `paths: { "@routtes/shared": ["../../packages/shared/src/index.ts"] }` em ambas as APIs. Task 0.5 verifica isso.

**`JwtModule.exports` na app-api:** O `JwtModule` é exportado pelo `AuthModule` da app-api para que módulos de features possam assinar tokens ou verificar JWTs internamente se necessário (ex: geração de invite tokens).
